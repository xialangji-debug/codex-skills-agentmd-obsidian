interface:
  display_name: "禅道Bug分拣"
  short_description: "只抓当前激活Bug并分拣、匹配记忆和判断修复资格"
  default_prompt: "Use $zentao-bug-triage in ASR3601/ASR3602 Crane/LVGL firmware workspaces to fetch only active Zentao bugs for the current branch by default, download their details and attachments, classify repair eligibility, and inspect selected items before editing."
policy:
  allow_implicit_invocation: true
