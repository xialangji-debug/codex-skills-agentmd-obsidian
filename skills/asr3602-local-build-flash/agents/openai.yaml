interface:
  display_name: "ASR3602 Local Build Flash"
  short_description: "本地编译并刷入普通固件，按需完成会话绑定的自动日志采集"
  default_prompt: "Use $asr3602-local-build-flash to build and flash the exact ASR artifact, then capture current-session CATStudio evidence when requested."
